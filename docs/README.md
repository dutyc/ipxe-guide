# 第一章：iPXE 是什么？—— 它不只是 PXE 的替代品，而是网络操作系统的入口  
# 第二章：从裸机到屏幕的 7 次跳跃 —— DHCP → TFTP → HTTP → kernel → initrd → rootfs → GUI  
# 第三章：UEFI vs Legacy —— 为什么你不能混用 .kpxe 和 .efi  
# 第四章：变量如何穿越时空？—— 从 dnsmasq 到内核的完整传递链  
# 第五章：企业级无盘方案 —— SAN Boot + iSCSI + 高可用  